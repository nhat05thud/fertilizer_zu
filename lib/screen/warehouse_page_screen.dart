import 'package:flutter/material.dart';

class WarehousePageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Warehouse Page"),
      ),
    );
  }
}
